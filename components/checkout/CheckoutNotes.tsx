"use client";

/**
 * Project notes are now part of the compact customer fields (one underline row),
 * so this component is intentionally a no-op for desktop. The mobile layout
 * still uses the full multi-line ProjectNotes textarea.
 */
export function CheckoutNotes() {
  return null;
}
